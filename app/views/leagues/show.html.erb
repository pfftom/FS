<% @league.teams.each do |team| %>
  <div><%= link_to team.name, [@league, team] %> | <%= team.user.user_name %></div>
<% end %>

<table>
  <%= render @standings %>
</table>

<div class="transactions">
  <%= render @league.newest_first_transactions %>
</div>
